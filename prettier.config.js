// to perform a forman in all files run this command on the desired folder
// " npx prettier --write . "

module.exports = {
    trailingComma: 'es5',
    tabWidth: 4,
    semi: false,
    singleQuote: true,
}
